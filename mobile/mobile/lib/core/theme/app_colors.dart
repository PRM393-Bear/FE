import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette
  static const Color primary = Color(0xFF006B2C);
  static const Color primaryDark = Color(0xFF004D1F);
  static const Color primaryLight = Color(0xFF4CAF77);

  // Secondary
  static const Color secondary = Color(0xFFFEA619);

  // Tertiary
  static const Color tertiary = Color(0xFF00685F);

  // Neutral
  static const Color neutral = Color(0xFF6E7B6C);

  // Background
  static const Color background = Color(0xFFF0F5EF); // xanh nhạt như design
  static const Color surface = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6E7B6C);
  static const Color textHint = Color(0xFFAAAAAA);

  // Border
  static const Color border = Color(0xFFDDE5DB);

  // Error
  static const Color error = Color(0xFFBA1A1A);
}